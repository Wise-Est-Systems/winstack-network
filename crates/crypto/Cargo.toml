[package]
name = "wise-crypto"
publish = false
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
sha2 = { workspace = true }
ed25519-dalek = { workspace = true }
rand = { workspace = true }
hex = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
# Used by the CanonStable determinism guard: Uuid is a signed-payload field
# type, so it must be declared canonicalization-stable here.
uuid = { workspace = true }

[lints]
workspace = true
