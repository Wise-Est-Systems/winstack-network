--- crates/win-format/src/lib.rs
+++ delete match arm b'{' | b'[' in brackets_balanced
@@ -174,17 +174,17 @@
                 b'\\' => escape = true,
                 b'"' => in_string = false,
                 _ => {},
             }
             continue;
         }
         match b {
             b'"' => in_string = true,
-            b'{' | b'[' => depth += 1,
+             /* ~ changed by cargo-mutants ~ */
             b'}' | b']' => {
                 depth -= 1;
                 if depth < 0 {
                     return false;
                 }
             },
             _ => {},
         }
