--- crates/win-format/src/lib.rs
+++ replace += with -= in brackets_balanced
@@ -174,17 +174,17 @@
                 b'\\' => escape = true,
                 b'"' => in_string = false,
                 _ => {},
             }
             continue;
         }
         match b {
             b'"' => in_string = true,
-            b'{' | b'[' => depth += 1,
+            b'{' | b'[' => depth -= /* ~ changed by cargo-mutants ~ */ 1,
             b'}' | b']' => {
                 depth -= 1;
                 if depth < 0 {
                     return false;
                 }
             },
             _ => {},
         }
