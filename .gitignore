/target
.wise/
*.proof.json
*.win
# Exception: the committed golden .win regression fixture must be tracked.
!crates/registry-core/tests/vectors/golden/golden.win
.vercel
.DS_Store

# Working-notes scratchpad. Outreach drafts, anchor-user shortlists, and other
# pre-public material live here. Never committed.
/private/

# Sample artifacts produced by `scripts/build-gallery.sh`. Regenerable.
/gallery/

# cargo-fuzz: corpus and crash artifacts (regenerable)
/fuzz/corpus/
/fuzz/artifacts/
/fuzz/target/

# cargo-mutants output (regenerable)
/mutants.out/
/mutants.out.old/
/mutants.out.*/
