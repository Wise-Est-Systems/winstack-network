--- crates/win-format/src/lib.rs
+++ delete match arm b'"' in brackets_balanced
@@ -167,17 +167,17 @@
     for b in s.bytes() {
         if escape {
             escape = false;
             continue;
         }
         if in_string {
             match b {
                 b'\\' => escape = true,
-                b'"' => in_string = false,
+                 /* ~ changed by cargo-mutants ~ */
                 _ => {},
             }
             continue;
         }
         match b {
             b'"' => in_string = true,
             b'{' | b'[' => depth += 1,
             b'}' | b']' => {
